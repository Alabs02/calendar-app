export { EThemes } from "./themes.enum";

export enum EColorVariant {
  ACCENT = 'accent',
  DEFAULT = "default",
  PRIMARY = 'primary',
  // RED = 'red',
  // GREEN = 'green',
  // INDIGO = 'indigo',
  SECONDARY = 'secondary'
}

export enum EButtonSize {
  SMALL = "sm",
  MEDIUM = "md",
  LARGE = "lg",
}

export enum ESetDateType {
  SET_TODAY = "set-today",
  SET_SELECTED_DAY = "set-selected-day",
  SET_CURRENT_MONTH = "set-current-month",
  SET_FIRST_DAY_OF_CURRENT_MONTH = "set-first-day-of-current-month",
}
